[project]
name = "admin-module"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gputil>=1.4.0",
    "psutil>=7.2.2",
    "pyadl>=0.1",
    "wmi>=1.5.1",
]
